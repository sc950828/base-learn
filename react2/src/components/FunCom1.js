const FunCom1 = (props) => {
  return (
    <div>
      <div>{props.title}</div>
      <div>user.name: {props.user.name}</div>
      <div>user.age: {props.user.age}</div>
    </div>
  );
};

export default FunCom1;
